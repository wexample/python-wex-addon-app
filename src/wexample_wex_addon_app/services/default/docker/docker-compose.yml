services:
  default:
    stdin_open: ${APP_DOCKER_COMPOSE_STDIN_OPEN:-true}
    tty: ${APP_DOCKER_COMPOSE_TTY:-true}
    restart: unless-stopped
    environment:
      - MANAGED_APP_ENV=${APP_ENV}
    networks:
      - wex_net
